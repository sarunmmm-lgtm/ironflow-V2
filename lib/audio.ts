export function playTone(freq = 760, duration = 0.16, volume = 0.14) { if (typeof window === "undefined") return; const AudioContext = window.AudioContext || (window as any).webkitAudioContext; if (!AudioContext) return; const ctx = new AudioContext(); const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.connect(gain); gain.connect(ctx.destination); osc.frequency.value = freq; osc.type = "sine"; gain.gain.setValueAtTime(0.0001, ctx.currentTime); gain.gain.exponentialRampToValueAtTime(volume, ctx.currentTime + 0.012); gain.gain.exponentialRampToValueAtTime(0.0001, ctx.currentTime + duration); osc.start(ctx.currentTime); osc.stop(ctx.currentTime + duration + 0.03); }
export function speakThai(text: string) { if (typeof window === "undefined" || !window.speechSynthesis) return; window.speechSynthesis.cancel(); const utterance = new SpeechSynthesisUtterance(text); utterance.lang = "th-TH"; utterance.rate = 1; utterance.pitch = 1.04; window.speechSynthesis.speak(utterance); }
export function playCountdown(secondsLeft: number) { if (secondsLeft === 10) { playTone(760, 0.16, 0.14); speakThai("เหลืออีกสิบวินาที"); } if (secondsLeft <= 5 && secondsLeft > 3) { playTone(980, 0.08, 0.16); window.setTimeout(() => playTone(980, 0.08, 0.13), 115); } if ([3, 2, 1].includes(secondsLeft)) { playTone(secondsLeft === 1 ? 1250 : 980, 0.16, 0.18); speakThai(String(secondsLeft)); } }
